<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="logMapper">
	<insert id="insertSearchLog">
		INSERT INTO what_they_search(
			search_key,
			user_ip,
			search_date,
			search_where
		)
		VALUES(
			#{search_key},
			#{user_ip},
			#{search_date},
			#{search_where}
		)
	</insert>
</mapper>